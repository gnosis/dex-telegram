// TODO: This is just temporary, coveralls fails if you don't cover ANY line. This PR doesn't implement test, but add the integrations
export function sum(a: number, b: number): number {
  return a + b
}
